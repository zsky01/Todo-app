import styled from "styled-components";

const Button = styled.button`
  background: none;
  border: none;
  outline: none;
  cursor: pointer;
  color: inherit;
  font-family: inherit;
  font-size: inherit;
`;

export {Button};