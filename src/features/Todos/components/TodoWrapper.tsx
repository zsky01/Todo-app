import styled from "styled-components";

const TodoWrapper = styled.div`
  padding: 0 1.2rem;
  width: 100%;
  background: var(--colors-item-bg);
  display: flex;
  flex-direction: row;
  align-items: center;
  cursor: text;
`;

export {TodoWrapper};